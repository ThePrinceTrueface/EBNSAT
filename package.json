{
	"name": "iptv-ebnst-server",
	"version": "1.0.0",
	"type": "module",
	"scripts": {
      "build": "echo Pas de build nécessaire && exit 0",
		"ts:build": "tsc",
		"start": "node index.js",
		"build:s": "esbuild ./index.ts --bundle --platform=node --outdir=build --format=esm --packages=external",
		"dev": "nodemon --watch build build/index.js",
		"watch": "esbuild ./index.ts --bundle --platform=node --outdir=build --format=esm --packages=external --watch"
	},
	"dependencies": {
      "dotenv": "^17.2.3",
		"ejs": "^3.1.10",
		"express": "^5.1.0",
		"express-ejs-layouts": "^2.5.1",
		"multer": "^2.0.2",
		"zod": "^4.1.12"
	}
}
